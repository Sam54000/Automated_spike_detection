clearvars -except d DE dat_irrit elec idx_elec labels_BIP discharges data clustering


ch = 85;
DataAnalysis = d;
[ValMinPos,idxMinPos] = min(discharges.MP,[],2);
pos = round(discharges.MP(clustering.class == 1 & idxMinPos == ch & discharges.MV(:,ch) == 1,ch).*data.fs);
win = 100;
[x, ~, ~] = bst_bandpass_hfilter(DataAnalysis(:,ch)',1024,6,0);

for i = 1:size(pos,1)
    window(i,:) = x(pos(i,1)-win:pos(i,1)+win)-mean(x);
end
test = abs(window);
plot(test')